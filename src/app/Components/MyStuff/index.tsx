import { Project } from "next/dist/build/swc";
import "./index.css";

type Project = {
  title: string;
  description: string;
  liveurl: string;
  githuburl: string;
  playstore: string;
};

const MyStuff = (props: Props) => {
  return <div></div>;
};

export default MyStuff;
